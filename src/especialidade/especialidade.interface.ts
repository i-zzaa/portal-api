export abstract class EspecialidadeServiceInterface {
  abstract createEspecialidade(): string;
  abstract updateEspecialidade(): string;
  abstract getEspecialidade(): string;
  abstract searchEspecialidade(): string;
}
